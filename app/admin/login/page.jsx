'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[m,setM]=useState(''),r=useRouter();async function go(x){x.preventDefault();const q=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:e,password:p})}),j=await q.json();q.ok?r.push('/admin'):setM(j.error)}return <main className="auth"><form className="card form" onSubmit={go}><div className="brand">COBRA<span>•</span>ADMIN</div><h2>Admin Login</h2><input className="input" type="email" value={e} onChange={x=>setE(x.target.value)} placeholder="Admin email"/><input className="input" type="password" value={p} onChange={x=>setP(x.target.value)} placeholder="Admin password"/><button className="btn">Login</button>{m&&<div className="notice">{m}</div>}</form></main>}
