import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import {verifyAdminToken,ADMIN_COOKIE} from '@/lib/admin-session';import {firebaseAdmin} from '@/lib/firebase-admin';import AdminShell from '@/components/AdminShell';export const dynamic='force-dynamic';export default async function Admin(){if(!verifyAdminToken(cookies().get(ADMIN_COOKIE)?.value))redirect('/admin/login');const db=firebaseAdmin().db,[u,p]=await Promise.all([db.ref('users').once('value'),db.ref('payments').once('value')]),us=u.val()||{},ps=p.val()||{},pending=Object.values(ps).filter(x=>x.status==='pending').length,premium=Object.values(us).filter(x=>x.premium&&x.premiumExpiresAt>Date.now()).length;return <AdminShell><h1>Admin Dashboard</h1><div className="grid"><div className="card"><div className="muted">Users</div><h2>{Object.keys(us).length}</h2></div><div className="card"><div className="muted">Premium</div><h2>{premium}</h2></div><div className="card"><div className="muted">Pending Payments</div><h2>{pending}</h2></div></div><div className="notice" style={{marginTop:15}}>Admin ID/password come only from Vercel Environment Variables.</div></AdminShell>}
