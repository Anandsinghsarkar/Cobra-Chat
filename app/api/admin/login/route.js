import {NextResponse} from 'next/server';import crypto from 'crypto';import {makeAdminToken,ADMIN_COOKIE} from '@/lib/admin-session';const eq=(a,b)=>{const x=Buffer.from(String(a||'')),y=Buffer.from(String(b||''));return x.length===y.length&&crypto.timingSafeEqual(x,y)};export async function POST(req){const{email,password}=await req.json();if(!eq(email,process.env.ADMIN_EMAIL)||!eq(password,process.env.ADMIN_PASSWORD))return NextResponse.json({error:'Invalid admin credentials'},{status:401});const r=NextResponse.json({ok:true});r.cookies.set(ADMIN_COOKIE,makeAdminToken(email),{httpOnly:true,secure:true,sameSite:'strict',path:'/',maxAge:43200});return r}
