'use client';import {useState} from 'react';import Link from 'next/link';import {signInWithEmailAndPassword,signInWithPopup} from 'firebase/auth';import {firebaseClient} from '@/lib/firebase-client';import {useRouter} from 'next/navigation';export default function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[m,setM]=useState(''),r=useRouter();async function go(x){x.preventDefault();try{await signInWithEmailAndPassword(firebaseClient().auth,e,p);r.push('/app')}catch(z){setM(z.message)}}async function google(){try{const c=firebaseClient(),x=await signInWithPopup(c.auth,c.googleProvider),t=await x.user.getIdToken();await fetch('/api/profile/bootstrap',{method:'POST',headers:{Authorization:'Bearer '+t}});r.push('/app')}catch(z){setM(z.message)}}return <main className="auth"><form className="card form" onSubmit={go}><div className="brand">COBRA<span>•</span>SOCIAL</div><h2>Login</h2><input className="input" type="email" placeholder="Email" value={e} onChange={x=>setE(x.target.value)} required/><input className="input" type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)} required/><button className="btn">Login</button><button className="btn secondary" type="button" onClick={google}>Continue with Google</button>{m&&<div className="notice">{m}</div>}<div className="row space"><Link className="muted" href="/forgot">Forgot password?</Link><Link className="muted" href="/register">Register</Link></div><Link className="muted" href="/admin/login">Admin login →</Link></form></main>}
