'use client';import {useEffect,useState} from 'react';import {onAuthStateChanged} from 'firebase/auth';import {useRouter} from 'next/navigation';import {firebaseClient} from '@/lib/firebase-client';export default function AuthGate({children}){const[r,setR]=useState(false),router=useRouter();useEffect(()=>onAuthStateChanged(firebaseClient().auth,u=>u?setR(true):router.replace('/login')),[router]);return r?children:<div className="auth"><div className="muted">Checking account…</div></div>}
