'use client';import {useState} from 'react';export default function A({id}){const[m,setM]=useState('');async function go(action){const r=await fetch('/api/admin/payment-action',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,action})}),j=await r.json();setM(j.message||j.error);if(r.ok)setTimeout(()=>location.reload(),350)}return <div><div className="row"><button className="btn" onClick={()=>go('approve')}>Approve</button><button className="btn danger" onClick={()=>go('reject')}>Reject</button></div>{m&&<span className="muted">{m}</span>}</div>}
