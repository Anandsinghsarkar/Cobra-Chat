import admin from 'firebase-admin';export function firebaseAdmin(){if(!admin.apps.length){const raw=process.env.FIREBASE_SERVICE_ACCOUNT_JSON;const databaseURL=process.env.FIREBASE_DATABASE_URL||process.env.NEXT_PUBLIC_FIREBASE_DATABASE_URL;if(!raw||!databaseURL)throw new Error('Firebase Admin env missing');admin.initializeApp({credential:admin.credential.cert(JSON.parse(raw)),databaseURL,storageBucket:process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET})}return{admin,auth:admin.auth(),db:admin.database()}}
