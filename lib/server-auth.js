import {firebaseAdmin} from './firebase-admin';export async function requireUser(req){const h=req.headers.get('authorization')||'',t=h.startsWith('Bearer ')?h.slice(7):'';if(!t)throw new Error('AUTH_REQUIRED');return firebaseAdmin().auth.verifyIdToken(t)}
