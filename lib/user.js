import {firebaseAdmin} from './firebase-admin';import {levelFromXp,rankFromXp} from './ranks';export async function saveUser(uid,patch={}){const r=firebaseAdmin().db.ref('users/'+uid),s=await r.once('value'),o=s.val()||{},xp=Number(patch.xp??o.xp??0),u={...o,...patch,xp,level:levelFromXp(xp),rank:(Object.prototype.hasOwnProperty.call(patch,'rankOverride') ? (patch.rankOverride||rankFromXp(xp)) : (o.rankOverride||rankFromXp(xp))),updatedAt:Date.now()};await r.set(u);return u}export const isPremium=u=>!!u?.premium&&Number(u.premiumExpiresAt||0)>Date.now();
